- [x] adding history feature
- [ ] giving ability to run bash commands
	```
	import os
	import subprocess

	# Using os.system()
	os.system("ls -l")
	
	# Using subprocess.run()
	subprocess.run(["ls", "-l"])
	```
